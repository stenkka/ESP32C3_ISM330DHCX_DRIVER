include $(APPDIR)/Make.defs

PROGNAME  = $(CONFIG_MEMS_APP_PROGNAME)
PRIORITY  = $(CONFIG_MEMS_APP_PRIORITY)
STACKSIZE = $(CONFIG_MEMS_APP_STACKSIZE)
MODULE    = $(CONFIG_MEMS_APP)

MAINSRC = MEMS_app.c

include $(APPDIR)/Application.mk
